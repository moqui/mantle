language: groovy

jdk:
  - oraclejdk7
  - openjdk7
# runs slowly and often fails, though it is faster on my local machine, commenting for now:
#  - oraclejdk8

before_script:
  - cd ..
  - git clone https://github.com/moqui/moqui.git moqui
  - cd moqui/runtime
  - git clone https://github.com/moqui/mantle.git mantle
  - cd ..

script:
  - gradle cleanAll load
  - gradle check
  - gradle runtime/mantle/mantle-usl:test --debug
